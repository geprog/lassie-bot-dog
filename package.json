{
  "dependencies": {
    "@gitbeaker/node": "^28.4.1",
    "dotenv": "^9.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.11.19",
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "build": "esbuild --outdir=dist/ --bundle --platform=node --target=node14 src/index.ts",
    "start": "nodemon"
  }
}
